import React from 'react';

const Secret = () => {
    return (
        <div className="container mt-5 text-danger">
            <h2>This is private route.Only access-able for administrator</h2>
        </div>
    );
};

export default Secret;